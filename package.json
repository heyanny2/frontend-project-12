{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Chat",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx start-server -s ./frontend/build",
    "build": "npm run build --prefix frontend",
    "postinstall": "cd frontend && npm ci"
  },
  "author": "Anna Prokopeva",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.9",
    "node-modules": "^1.0.1"
  }
}
